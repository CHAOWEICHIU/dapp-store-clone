{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "remove:node_modules": "lerna run remove:node_modules"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
